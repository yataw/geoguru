const server = {port: window.port, origin: window.origin}
const socket = window.socket
const $ = window.jQuery

const init = {}

const types = {}

types.ServerEvents = {
    SIGNIN: "signin",
    SIGNOUT: "signout",
    JOIN: "join",
    VOTE: "vote",
    START: "matchstart",
    END: "matchend",
}

/**
 * @type {string}
 */
types.SocketId;

/**
 * @type {{coutry: string, city: string}}
 */
types.Task;

export {server, socket, $, types, init}