const server = {port: window.port, origin: window.origin}
const socket = window.socket
const $ = window.jQuery

const types = {}

types.ServerEvents = {
    START: 'matchstart',
    END: 'matchend',
}

/**
 * @type {{coutry: string, city: string}}
 */
types.Task;

export {server, socket, $, types}